[package]
name = "core_bin"
version = "0.1.0"
edition = "2024"

[dependencies]
core_lib = { path = "../core_lib" }

tracing = "0.1.44"
tracing-subscriber = { workspace = true, features = ["env-filter"] }
